[package]
name = "webtoons"
version = "0.1.0"
edition = "2021"

[dependencies]
aidoku = { git = "https://github.com/Aidoku/aidoku-rs" }

[profile.release]
strip = true
lto = true
opt-level = "z"

[lib]
crate-type = ["cdylib"]
