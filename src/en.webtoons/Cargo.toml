[package]
name = "webtoons"
version = "0.1.0"
edition = "2024"

[dependencies]
aidoku = { git = "https://github.com/Aidoku/aidoku-rs", tag = "v0.5.1" }

[profile.release]
strip = true
lto = true
opt-level = "z"

[lib]
crate-type = ["cdylib"]
